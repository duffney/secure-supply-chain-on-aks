---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: azure-voting
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: azure-voting
  policy:
    semver:
      range: '>=v0.1-alpha-0'
